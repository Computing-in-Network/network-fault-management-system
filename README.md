# monitor-dynamic-topo

This repository combines two existing projects into one workspace:

- `components/monitor` (source: `https://github.com/Computing-in-Network/monitor.git`)
- `components/dynamic-topo` (source: `https://github.com/Computing-in-Network/dynamic-topo.git`)

## Layout

```text
monitor-dynamic-topo/
  components/
    monitor/
    dynamic-topo/
```

## Notes

- The original repositories were copied without their `.git` directories.
- Each component keeps its original file structure.
